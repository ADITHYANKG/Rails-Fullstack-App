<% content_for :title, "Categories" %>

<div class="w-full p-6">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <!-- Header Section -->
  <div class="flex justify-between items-center mb-6">
    <h1 class="font-bold text-3xl sm:text-4xl">Categories</h1>
    <%= link_to "New Category", new_admin_category_path, class: "rounded-md px-4 py-2.5 bg-black/100 hover:bg-black/75 text-white font-medium" %>
  </div>

  <!-- Responsive Table -->
  <div class="overflow-x-auto bg-white shadow-md rounded-lg p-4">
    <table class="w-full table-auto min-w-full border-collapse">
      <thead class="bg-gray-200 border-b">
        <tr>
          <th class="px-6 py-4 text-left text-sm font-medium text-gray-800">Image</th>
          <th class="px-6 py-4 text-left text-sm font-medium text-gray-800">Name</th>
          <th class="px-6 py-4 text-left text-sm font-medium text-gray-800">Description</th>
        </tr>
      </thead>
      <tbody>
        <% @admin_categories.each do |category| %>
          <tr class="border-b even:bg-gray-100 hover:bg-gray-50 transition">
            <!-- Category Image -->
            <td class="px-6 py-4">
              <%= category.image.present? ? image_tag(category.image.variant(:thumb), class: "w-12 h-12 object-cover rounded-md") : image_tag("http://via.placeholder.com/50", class: "w-12 h-12 object-cover rounded-md") %>
            </td>

            <!-- Category Name -->
            <td class="px-6 py-4">
              <%= link_to category.name, edit_admin_category_path(category), class: "text-blue-600 hover:underline font-medium" %>
            </td>

            <!-- Category Description -->
            <td class="px-6 py-4 text-gray-700 text-sm sm:text-base">
              <%= category.description.truncate(50) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
